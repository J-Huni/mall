<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  	<mapper namespace="com.js.mapper.signupMapper">
  	  	<insert id="signup">
  			insert 
  			into mall_login(id, pw, name, email, address, pnum, t_div) 
  			values(#{id}, #{pw}, #{name}, #{email}, #{address},#{pnum},2)
    	</insert>
  	</mapper>